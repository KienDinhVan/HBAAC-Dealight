apiVersion: v1
kind: Service
metadata:
  name: grafana
  annotations:
    cloud.google.com/backend-config: '{"default":"grafana"}'
spec:
  selector:
    app: grafana
  ports:
    - port: 3000
      targetPort: 3000
