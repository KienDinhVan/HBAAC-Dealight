apiVersion: apps/v1
kind: Deployment
metadata:
  name: airflow-scheduler
spec:
  replicas: 1
  selector:
    matchLabels:
      app: airflow-scheduler
  template:
    metadata:
      labels:
        app: airflow-scheduler
    spec:
      serviceAccountName: airflow
      containers:
        - name: scheduler
          image: asia-southeast1-docker.pkg.dev/gen-lang-client-0222711301/dealight/airflow:bootstrap
          args: ["scheduler"]
          envFrom:
            - configMapRef:
                name: platform-config
            - secretRef:
                name: platform-secrets
          livenessProbe:
            exec:
              command: ["airflow", "jobs", "check", "--job-type", "SchedulerJob", "--local"]
            initialDelaySeconds: 120
            periodSeconds: 60
            # Airflow CLI mất vài giây khởi động — timeout mặc định 1s giết scheduler liên tục.
            timeoutSeconds: 30
            failureThreshold: 5
          resources:
            requests:
              cpu: 500m
              memory: 2Gi
