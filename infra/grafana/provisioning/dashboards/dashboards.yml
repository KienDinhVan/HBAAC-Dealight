apiVersion: 1

providers:
  - name: sku-demand-observability
    orgId: 1
    folder: Forecasting
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
